# Лабораторная работа №4

**University:** [ITMO University](https://itmo.ru/ru/)  
**Faculty:** [FICT](https://fict.itmo.ru)  
**Course:** [Cloud platforms as the basis of technology entrepreneurship](https://)  
**Year:** 2025/2026  
**Group:** U4125  
**Author:** Исайкин Евгений Дмитриевич  
**Lab:** Lab4  
**Date of create:**  
**Date of finished:**  

## 1. Описание приложения

В качестве приложения рассматривается **FlowCraft AI** — AI Copilot для генерации аналитических workflow для Langflow по текстовому запросу пользователя.

Приложение предназначено для аналитиков, продуктовых команд и бизнес-пользователей, которым нужно быстро собрать pipeline для проверки гипотез на данных без ручного соединения компонентов в Langflow.

Пользователь загружает датасет или указывает источник данных, после чего описывает аналитическую задачу на естественном языке, например:

> У меня есть датасет по клиентским транзакциям. Нужно проверить гипотезу, что пользователи с высоким средним чеком чаще возвращаются в течение 30 дней. Отфильтруй клиентов по сегментам, посчитай повторные покупки и сравни retention между группами.

После этого система анализирует запрос, выделяет цель исследования, проверяемую гипотезу, необходимые фильтры, метрики и шаги обработки данных. Затем FlowCraft AI подбирает подходящие компоненты Langflow, собирает workflow для загрузки данных, фильтрации, расчета метрик, сравнения групп, интерпретации результата через LLM и экспортирует готовый граф в формате `flow.json`.

---

## 2. Базовая функциональность MVP

MVP-версия приложения включает:

1. загрузку датасета или выбор источника данных;
2. ввод аналитической задачи на естественном языке;
3. определение проверяемой гипотезы;
4. выделение фильтров, метрик и необходимых преобразований данных;
5. подбор подходящего шаблона аналитического workflow;
6. сборку графа Langflow из совместимых компонентов;
7. заполнение параметров компонентов;
8. проверку корректности структуры workflow;
9. генерацию файла `flow.json`;
10. сохранение истории запросов;
11. скачивание готового результата пользователем.

Для MVP не предполагается генерация любых произвольных аналитических pipeline. На первом этапе достаточно поддержать несколько типовых сценариев.

| Сценарий | Описание |
|---|---|
| Фильтрация данных | Отбор строк по заданным условиям: период, сегмент, категория, регион |
| Расчет метрик | Подсчет среднего чека, retention, conversion rate, количества событий |
| Сравнение групп | Проверка различий между сегментами пользователей или продуктов |
| Интерпретация результата | Использование LLM для краткого объяснения полученных метрик |
| Экспорт workflow | Формирование `flow.json` для импорта в Langflow |

Такое ограничение делает MVP реалистичным: система не пытается заменить аналитика полностью, а помогает быстро собрать технический workflow для проверки типовых гипотез.

---

## 3. Состояния приложения

Для приложения рассматриваются три состояния:

1. **Этап 1 MVP** — проверка гипотезы с минимальной инфраструктурой.
2. **Этап 2 тестирование** — подключение партнеров и переход к асинхронной обработке.
3. **Этап 3 production** — масштабирование, безопасность, мониторинг и контроль стоимости.

---

# 4. Этап 1 MVP

## 4.1. Предполагаемая нагрузка

| Параметр | Значение |
|---|---:|
| Пользователей в месяц | 100 |
| Среднее число генераций на пользователя | 2 |
| Генераций workflow в месяц | 200 |
| Новых AI-генераций | 200 |

## 4.2. Схема инфраструктуры

<img width="1542" height="751" alt="stage 1" src="https://github.com/user-attachments/assets/a60deffd-05c2-4087-8c8d-28534fd76bc5" />

## 4.3. Описание работы системы

1. Пользователь открывает Web UI в браузере.
2. Пользователь загружает датасет и вводит аналитический запрос для проверки гипотезы.
3. Web UI отправляет запрос в Backend API.
4. Backend API работает в Cloud Run.
5. Backend API проверяет корректность запроса, формат датасета и ограничения по размеру файла.
6. Backend API отправляет в Vertex AI / Gemini описание гипотезы, доступные поля датасета и ограничения по workflow.
7. AI-модель возвращает структуру аналитического workflow.
8. Backend API определяет тип аналитической задачи, выбирает шаблон, заполняет параметры фильтрации, расчета метрик и валидирует результат.
9. Готовый `flow.json` сохраняется в Cloud Storage.
10. Данные о пользователе, запросе, датасете и результате сохраняются в Firestore.
11. Пользователь получает ссылку на скачивание `flow.json`.
12. Логи приложения отправляются в Cloud Logging.

## 4.4. Используемые ресурсы

| Ресурс | Назначение |
|---|---|
| Web UI | Интерфейс для ввода запроса и скачивания результата |
| Cloud Run | Запуск Backend API без управления серверами |
| Vertex AI / Gemini | Генерация структуры аналитического workflow |
| Cloud Storage | Хранение загруженных датасетов, `flow.json` и связанных файлов |
| Firestore | Хранение пользователей, запросов, истории, статусов и метаданных датасетов |
| Cloud Logging | Базовое логирование ошибок и времени генерации |

## 4.5. Обоснование выбора ресурсов

**Cloud Run** выбран для MVP, потому что он позволяет запускать backend без ручного управления серверами. Это удобно для начального этапа, когда нагрузка неизвестна и может быть нерегулярной. Cloud Run тарифицируется по фактическому использованию ресурсов после учета free tier, что хорошо подходит для MVP.

**Vertex AI / Gemini** используется как готовая LLM. Для MVP нецелесообразно обучать собственную модель или поднимать GPU-инфраструктуру. Готовый API позволяет быстро проверить идею продукта.

**Firestore** подходит для MVP, так как данные имеют простую структуру: пользователи, запросы, история генераций, статусы и ссылки на файлы.

**Cloud Storage** используется для хранения загруженных датасетов и итоговых `flow.json`, потому что это файловые артефакты. Хранить такие файлы в объектном хранилище логичнее, чем в базе данных.

**Cloud Logging** нужен для базовой отладки: ошибок backend, времени обработки запроса и проблем при генерации.

---

# 5. Этап 2 тестирование

## 5.1. Предполагаемая нагрузка

| Параметр | Значение |
|---|---:|
| Пользователей в месяц | 500 |
| Среднее число генераций на пользователя | 5 |
| Генераций workflow в месяц | 2 500 |
| Новых AI-генераций | 2 250 |

## 5.2. Схема инфраструктуры

<img width="1683" height="789" alt="stage 2" src="https://github.com/user-attachments/assets/c42eaf22-84fc-4077-b33e-7ad36f9b97f4" />

## 5.3. Описание работы системы

1. Пользователь открывает Web UI.
2. Пользователь отправляет аналитический запрос и параметры датасета.
3. Backend API Service принимает запрос.
4. Backend проверяет аутентификацию, лимиты, корректность аналитического запроса и параметры датасета.
5. Backend сохраняет запрос, метаданные датасета и создает задачу в Firestore.
6. Backend публикует задачу в Pub/Sub.
7. Generation Worker получает задачу из очереди.
8. Worker обращается к Component Registry и выбирает компоненты для загрузки данных, фильтрации, расчета метрик и интерпретации результата.
9. Component Registry возвращает информацию о доступных компонентах Langflow, их параметрах, типах входов/выходов и правилах совместимости.
10. Worker отправляет подготовленный промпт в Vertex AI / Gemini для построения аналитического workflow.
11. AI-модель возвращает структуру аналитического workflow.
12. Worker валидирует структуру графа, обязательные параметры и совместимость компонентов.
13. Worker сохраняет готовый `flow.json` в Cloud Storage.
14. Worker обновляет статус задачи в Firestore.
15. Пользователь получает уведомление о готовности и скачивает `flow.json`.
16. Cloud Logging и Cloud Monitoring собирают логи, метрики и ошибки.

## 5.4. Используемые ресурсы

| Ресурс | Назначение |
|---|---|
| Cloud Run API Service | Прием пользовательских запросов |
| Pub/Sub | Очередь задач на генерацию workflow |
| Cloud Run Generation Worker | Фоновая генерация и валидация workflow |
| Vertex AI / Gemini | Генерация структуры аналитического workflow |
| Firestore | Хранение пользователей, запросов, статусов задач и истории |
| Component Registry | Справочник компонентов Langflow для загрузки, фильтрации, расчета метрик и интерпретации результатов |
| Cloud Storage | Хранение датасетов, `flow.json`, шаблонов и файлов |
| Cloud Logging / Monitoring | Логи, метрики, ошибки и алерты |

## 5.5. Обоснование выбора ресурсов

На втором этапе синхронная обработка становится менее удобной. Генерация workflow может занимать несколько секунд или дольше, особенно если требуется повторная валидация. Поэтому добавляется **Pub/Sub** и отдельный **Generation Worker**.

Разделение API и worker дает несколько преимуществ:

- пользовательский API не блокируется долгой генерацией;
- задачи можно повторять при ошибках;
- worker можно масштабировать отдельно от API;
- система становится устойчивее к пиковым нагрузкам;
- появляется возможность хранить промежуточные статусы задач.

**Component Registry** нужен для того, чтобы AI-модель не генерировала произвольные компоненты. В нем хранятся допустимые компоненты Langflow для загрузки данных, фильтрации, расчета метрик, сравнения групп, интерпретации результата, а также параметры и правила совместимости.

---

# 6. Этап 3 production

## 6.1. Предполагаемая нагрузка

| Параметр | Значение |
|---|---:|
| Пользователей в месяц | 5 000 |
| Среднее число генераций на пользователя | 8 |
| Генераций workflow в месяц | 40 000 |
| Новых AI-генераций | 30 000 |

## 6.2. Схема инфраструктуры

<img width="1536" height="1024" alt="stage 3" src="https://github.com/user-attachments/assets/f2d52fd6-9f4e-4341-a3d7-1f9244041806" />

## 6.3. Описание работы системы

1. Пользователь или сотрудник партнерской организации открывает Web UI.
2. Запрос проходит через Cloud CDN и HTTPS Load Balancer.
3. API Gateway маршрутизирует запрос к нужной версии API.
4. Backend API Service проверяет аутентификацию и права доступа через Firebase Auth / IAM.
5. Backend проверяет лимиты пользователя, валидирует запрос и параметры датасета.
6. Backend сохраняет запрос и создает задачу в базе данных.
7. Backend публикует задачу в Pub/Sub.
8. Generation Worker получает задачу из очереди.
9. Worker проверяет наличие похожего ранее построенного workflow в Memorystore Redis.
10. Если подходящий workflow есть в кэше, он используется как основа для нового результата.
11. Если результата нет, Worker получает информацию из Component Registry.
12. Worker формирует промпт с учетом гипотезы, полей датасета, фильтров и метрик, после чего отправляет его в Vertex AI / Gemini.
13. AI-модель возвращает структуру аналитического workflow.
14. Worker валидирует результат, проверяет совместимость аналитических компонентов и собирает итоговый `flow.json`.
15. Worker сохраняет `flow.json` в Cloud Storage.
16. Метаданные результата сохраняются в Cloud SQL / Firestore.
17. Результат может быть добавлен в Memorystore Redis для повторного использования.
18. Backend возвращает пользователю статус и ссылку на скачивание.
19. Cloud Logging, Cloud Monitoring и Error Reporting собирают логи, метрики и ошибки.
20. Secret Manager хранит токены, ключи и конфигурационные секреты.

## 6.4. Используемые ресурсы

| Ресурс | Назначение |
|---|---|
| Cloud CDN | Ускорение отдачи статического контента |
| HTTPS Load Balancer | Единая точка входа и распределение запросов |
| API Gateway | Маршрутизация, лимиты, версии API |
| Firebase Auth / IAM | Аутентификация и управление доступом |
| Cloud Run API Service | Основной backend для пользовательских запросов |
| Pub/Sub | Асинхронная очередь генерации |
| Cloud Run Generation Worker | Генерация, валидация и сохранение workflow |
| Vertex AI / Gemini | LLM для генерации аналитического workflow и интерпретации результата |
| Cloud SQL / Firestore | Основная база данных |
| Cloud Storage | Хранение датасетов, `flow.json`, шаблонов и файлов |
| Component Registry | Справочник компонентов и правил совместимости |
| Memorystore Redis | Кэширование готовых и похожих результатов |
| Cloud Logging / Monitoring / Error Reporting | Наблюдаемость, алерты и анализ ошибок |
| Secret Manager | Управление секретами |

## 6.5. Обоснование выбора ресурсов

**API Gateway** добавляется на production-этапе, потому что появляется необходимость управлять маршрутизацией, лимитами, версиями API и доступом.

**Cloud CDN и HTTPS Load Balancer** нужны для production-сценария, где появляются пользователи из разных организаций, а приложение должно иметь стабильную точку входа.

**Memorystore Redis** добавляется только на production. Он используется для кэширования похожих результатов и снижения количества дорогих AI-вызовов.

**Cloud SQL / Firestore** используются для хранения пользователей, организаций, ролей, запросов, истории, статусов задач и метаданных workflow. На ранних этапах достаточно Firestore, но на production можно рассмотреть Cloud SQL, если потребуется более строгая реляционная модель данных.

**Cloud Logging, Cloud Monitoring и Error Reporting** нужны для наблюдаемости production-системы: отслеживания ошибок, задержек, количества генераций, отказов worker-сервисов и стоимости эксплуатации.

---

# 7. Расчет стоимости одного AI-запроса

Для учебной оценки используется ставка Gemini Flash-класса. Для упрощенного расчета примем:

| Параметр | Значение |
|---|---:|
| Input tokens | 10 000 |
| Output tokens | 4 000 |
| Стоимость input | $0.25 / 1M tokens |
| Стоимость output | $1.50 / 1M tokens |

Расчет:

| Часть | Формула | Стоимость |
|---|---:|---:|
| Input | 10 000 / 1 000 000 × $0.25 | $0.0025 |
| Output | 4 000 / 1 000 000 × $1.50 | $0.0060 |
| Базовая стоимость генерации |  | $0.0085 |
| Запас на повторную валидацию и служебные вызовы | +30% | $0.0026 |
| **Итого за одну новую генерацию** |  | **~$0.011** |

Для дальнейших расчетов используется округленное значение: **$0.011 за одну новую генерацию workflow**.

---

# 8. Экономическая модель по этапам

## 8.1. AI-расходы

| Этап | Генераций/мес. | Cache hit | Новых AI-генераций | Стоимость одной новой генерации | AI-расходы |
|---|---:|---:|---:|---:|---:|
| Этап 1 MVP | 200 | 0% | 200 | $0.011 | ~$2.2 |
| Этап 2 тестирование | 2 500 | 10% | 2 250 | $0.011 | ~$24.8 |
| Этап 3 production | 40 000 | 25% | 30 000 | $0.011 | ~$330 |

## 8.2. Инфраструктурные расходы

| Ресурс | Этап 1 MVP | Этап 2 тестирование | Этап 3 production |
|---|---:|---:|---:|
| Cloud Run API | $0–10 | $10–25 | $50–150 |
| Cloud Run Worker | — | $10–25 | $50–200 |
| Vertex AI / Gemini | ~$2 | ~$25 | ~$330 |
| Firestore / Cloud SQL | $0–5 | $5–20 | $80–250 |
| Cloud Storage | $1–5 | $5–15 | $10–100 |
| Pub/Sub | — | $0–5 | $5–20 |
| API Gateway | — | — | $0–20 |
| HTTPS Load Balancer + CDN | — | — | $20–100 |
| Memorystore Redis | — | — | $50–150 |
| Cloud Logging / Monitoring | $0 | $0–10 | $20–70 |
| Secret Manager | — | $1–3 | $2–10 |
| **Итого** | **~$3–22** | **~$56–128** | **~$617–1 400** |

## 8.3. Итоговая таблица

| Этап | Пользователей/мес. | Генераций/мес. | Основной подход | Примерная стоимость |
|---|---:|---:|---|---:|
| Этап 1 MVP | 100 | 200 | Монолитный backend на Cloud Run | ~$3–22 / мес. |
| Этап 2 тестирование | 500 | 2 500 | API + очередь + worker | ~$56–128 / мес. |
| Этап 3 production | 5 000 | 40 000 | CDN + API Gateway + worker + кэш + мониторинг | ~$617–1 400 / мес. |

---

# 9. Обоснование экономической модели

Основная статья расходов приложения — обращения к AI-модели. Хранение датасетов и `flow.json`, работа backend и база данных стоят существенно меньше, чем большое количество LLM-вызовов.

Поэтому для production особенно важны:

1. кэширование похожих результатов;
2. ограничение размера промпта;
3. ограничение числа генераций на пользователя;
4. использование шаблонов аналитических workflow вместо полной генерации графа с нуля;
5. повторное использование Component Registry;
6. отдельная валидация результата без лишних LLM-вызовов;
7. мониторинг стоимости одного пользователя.

На MVP расходы минимальны, так как Cloud Run, Firestore, Cloud Storage и Cloud Logging при небольшой нагрузке стоят незначительно. На стадии тестирования расходы растут из-за увеличения числа AI-запросов и появления отдельного worker-сервиса. На production основной рост связан с AI-вызовами, кэшированием, мониторингом, балансировкой и базой данных.
